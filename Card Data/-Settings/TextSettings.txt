# Start comment lines with the pound (#) symbol
# These settings allow you to change the way the generator outputs text
# You cannot add your own settings! Only the ones here may be modified
# Each setting has its own parameters. Only values within these parameters will be recognized
# If a setting is left blank, then the default value will be used

# The color of text your Villain uses
# Accepts: RGB color in hexcode (like #XXXXXX). Default: #ffffff
textColor:#d42323